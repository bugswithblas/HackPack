#DISTRO
cat /etc/issue
cat /etc/*-release
cat /etc/lsb-release      #DEBIAN
cat /etc/redhat-release   #REDHAT

-------------------------------------------------------------------------------------------------------------------------

#KERNEL
cat /proc/version
uname -a
uname -mrs
rpm -q kernel
dmesg | grep Linux
ls /boot | grep vmlinuz-

-------------------------------------------------------------------------------------------------------------------------

#ENVIRONMENT
cat /etc/profile
cat /etc/bashrc
cat ~/.bash_profile
cat ~/.bashrc
cat ~/.bash_logout
env
set
